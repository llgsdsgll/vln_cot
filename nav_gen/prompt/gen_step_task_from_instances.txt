[system]:
You are good at guiding the way. Now you need to generate step-by-step navigation instructions based on ranked visible object instances.
[user]:
The INPUT is a dictionary. The format is as follows:
```
{
  "target": {
    "category": The final navigation target category,
    "object_id": The specific target instance id when available
  },
  "step_0": {
    "action": The action of this step,
    "all_tag_rankings": [
      {
        "tag": A visible object instance id such as "radiator_10",
        "count": In how many front-view frames this exact instance appears in this step,
        "category": The semantic category of this instance
      },
      ...
    ]
  },
  ...
}
```
Please generate one complete, easy-to-understand, and accurate navigation instruction from the INPUT. Pay attention to the following requirements:

- Each `tag` is a specific object instance id. Different ids such as `chair_150` and `chair_152` are different objects and must not be merged when you reason about visibility.
- The `all_tag_rankings` list of each step is already sorted from the most frequently visible instance to the less visible ones. Prefer higher-ranked instances as anchors.
- Use the `all_tag_rankings` of each step to choose one most appropriate anchor for that step.
- For `move_forward`, prefer a stable scene-defining or route-defining object instance from the ranking.
- For `turn_left`, `turn_right`, `make a left turn`, `make a right turn`, and `zigzag`, prefer a nearby object instance from the ranking that can naturally serve as a turning reference.
- The order of actions in the final instruction must match the step order in the INPUT, and the number of described steps must stay the same.
- The final step must explicitly end at the target category in `target["category"]`.
- In the final natural-language instruction, do not copy raw ids like `radiator_10` verbatim unless it is truly necessary. Use natural wording such as "the radiator" or "the desk" while still respecting the ranked instance evidence.
- Your output should only be the instruction. The instruction should not contain words such as "step".

Here is an example:
```
INPUT:
{
  "target": {"category": "plant", "object_id": "plant_13"},
  "step_0": {
    "action": "move_forward",
    "all_tag_rankings": [
      {"tag": "radiator_10", "count": 12, "category": "radiator"},
      {"tag": "wardrobe_11", "count": 9, "category": "wardrobe"}
    ]
  },
  "step_1": {
    "action": "make a left turn",
    "all_tag_rankings": [
      {"tag": "wardrobe_11", "count": 6, "category": "wardrobe"},
      {"tag": "lamp_20", "count": 3, "category": "lamp"}
    ]
  },
  "step_2": {
    "action": "move_forward",
    "all_tag_rankings": [
      {"tag": "desk_42", "count": 7, "category": "desk"},
      {"tag": "plant_13", "count": 5, "category": "plant"}
    ]
  }
}

OUTPUT:
Move forward past the radiator, make a left turn by the wardrobe, then continue toward the plant and stop there.
```
Please get the output according to the above prompts and the following INPUT. Think step by step.
INPUT:
