# 更新说明（2026-04-03）

## 概述

截至 `2026-04-03`，当前项目目录已经完成一轮按用途的分类整理，并把 Qwen 标注主流程从其他辅助脚本中分离出来。

这次更新的核心目标是：

- 明确代码分层
- 保留原有启动方式
- 统一 Qwen 标注相关代码入口
- 方便后续做版本管理和维护

## 当前目录结构

```text
data_generation_pipline/
├── .gitignore
├── README.md
├── run_annotate_vln_episode.sh
├── qwen_annotation/
│   ├── README.md
│   ├── UPDATE_2026-04-03.md
│   ├── annotate_vln_episode.py
│   ├── qwen_api.py
│   └── run_annotate_vln_episode.sh
├── tools/
│   ├── check_api.py
│   ├── compare_scene_overlap.py
│   └── generate_vln_video.py
├── frames/
├── output/
├── habitat-lab/
└── VLN-CE/
```

## 目录职责说明

### 根目录

- `README.md`
  当前项目的总览说明，描述目录分类和推荐查看入口。
- `run_annotate_vln_episode.sh`
  兼容入口脚本。仍然支持在项目根目录直接运行，内部会转发到 `qwen_annotation/run_annotate_vln_episode.sh`。
- `.gitignore`
  当前 Git 策略：保留 `qwen_annotation/`、`tools/`、根目录 `README.md` 和根目录 `.sh` 文件；其他大型目录默认忽略。

### `qwen_annotation/`

当前正在使用的“大模型生成标注”主流程代码都集中在这里。

- `qwen_annotation/README.md`
  当前 Qwen 标注流程的使用说明，包括：
  - 基本运行方式
  - thinking 参数开关
  - `REUSE_RUN_DIR` 用法
  - 输出目录说明
- `qwen_annotation/run_annotate_vln_episode.sh`
  主启动脚本，支持环境变量配置。
- `qwen_annotation/annotate_vln_episode.py`
  Qwen 标注主流程脚本。
- `qwen_annotation/qwen_api.py`
  Qwen OpenAI-compatible 调用封装。

### `tools/`

当前和 Qwen 标注主流程分开的辅助工具脚本。

- `tools/generate_vln_video.py`
  使用 Habitat 渲染 VLN episode 视频和帧。
- `tools/compare_scene_overlap.py`
  检查 split 文件和场景目录的重叠情况。
- `tools/check_api.py`
  用于快速检查 Habitat API 可用性。

### 其他目录

- `frames/`
  输入视频和逐帧图像。
- `output/`
  所有运行输出、缓存、原始响应和标注结果。
- `habitat-lab/`
  外部依赖工程，未纳入当前自写标注主流程。
- `VLN-CE/`
  外部依赖工程，未纳入当前自写标注主流程。

## 当前代码功能说明

### 1. `qwen_annotation/annotate_vln_episode.py`

这是当前最核心的脚本，负责整条标注流水线。

主要功能：

- 读取 R2R episode 信息
- 读取完整第一人称视频和逐帧图像
- 基于 instruction 和完整视频进行子任务划分
- 基于完整帧序列进一步细化子任务边界
- 将每一帧映射到对应 subtask
- 对每一帧中的 object / region 做视觉定位
- 生成逐帧 JSON 结果
- 生成汇总 JSON
- 保存逐步进度文件
- 保存逐帧可视化结果
- 支持失败重试和断点续跑

当前实现特性：

- 输出目录自动带时间戳
- 每一帧处理后立即保存，不需要等全部完成
- 单帧失败最多重试 3 次
- 重试失败后记录错误并继续下一帧
- 支持 `REUSE_RUN_DIR`
- 支持记录原始模型返回文本和完整 response 结构

当前实体输出策略：

- `object`
  输出 `visible`、`center`、`bbox`、`distance_m`
- `region`
  输出 `visible`、`center`、`distance_range_m`

当前可视化策略：

- 只给 `object` 画 bounding box
- `region` 不再画 bounding box

### 2. `qwen_annotation/qwen_api.py`

这是当前调用 Qwen 的统一封装层。

主要功能：

- 构建 DashScope/OpenAI-compatible client
- 把本地图像编码成 `data URL`
- 把本地视频编码成 `data URL`
- 发送 text / image / video 多模态请求
- 支持普通文本返回
- 支持 JSON 返回
- 保存原始文本输出
- 保存完整 response 调试信息

当前支持的可配置参数：

- `temperature`
- `top_p`
- `result_format`
- `enable_thinking`
- `thinking_budget`

说明：

- 当前 thinking 不是写死的，已经做成可配置项
- 默认模型是 `qwen3.6-plus`

### 3. `qwen_annotation/run_annotate_vln_episode.sh`

这是当前推荐使用的主入口脚本。

主要功能：

- 统一设置默认路径
- 统一设置默认模型
- 把环境变量转成 Python 参数
- 支持开启 / 关闭 thinking
- 支持设置 `thinking_budget`
- 支持设置 `top_p`、`temperature`、`result_format`
- 支持 `REUSE_RUN_DIR`

支持的典型环境变量：

- `QWEN_THINKING`
- `QWEN_THINKING_BUDGET`
- `QWEN_TEMPERATURE`
- `QWEN_TOP_P`
- `QWEN_RESULT_FORMAT`
- `REUSE_RUN_DIR`
- `SEGMENT_MODEL`
- `LOCALIZE_MODEL`

### 4. `tools/generate_vln_video.py`

辅助生成 VLN episode 对应视频和图像帧。

主要功能：

- 读取 `train.json` 和 `train_gt.json`
- 根据 ground-truth action 还原 agent 运动
- 通过 Habitat 渲染 RGB / semantic 视角
- 导出视频和帧

### 5. `tools/compare_scene_overlap.py`

辅助检查 split 文件与场景目录的一致性。

主要功能：

- 读取 split 文件
- 提取场景 ID
- 读取场景目录
- 统计 overlap 数量并输出

### 6. `tools/check_api.py`

辅助做 Habitat API 快速探测。

主要功能：

- 查看 `habitat_sim` 暴露的传感器 / camera 相关接口
- 用于调试环境和 API 可用性

## 当前使用方式

### 根目录兼容运行

```bash
./run_annotate_vln_episode.sh
```

### 开启 thinking

```bash
QWEN_THINKING=true \
QWEN_THINKING_BUDGET=4000 \
QWEN_TOP_P=0.8 \
QWEN_TEMPERATURE=0.7 \
QWEN_RESULT_FORMAT=message \
./run_annotate_vln_episode.sh
```

### 使用 `REUSE_RUN_DIR`

```bash
REUSE_RUN_DIR=/home/gs/my_project/data_generation_pipline/output/qwen_annotations/episode_1_20260403_162938 \
./run_annotate_vln_episode.sh
```

说明：

- `REUSE_RUN_DIR` 不只是复用 subtasks
- 还会复用旧 run 中已有的 `frame_results`
- 已经完成的帧会被直接跳过

## 当前 Git 管理策略

当前目录已经执行：

- `git init`

当前 `.gitignore` 策略是：

- 保留 `qwen_annotation/`
- 保留 `tools/`
- 保留根目录 `README.md`
- 保留根目录 `.sh`
- 忽略 `frames/`
- 忽略 `output/`
- 忽略 `habitat-lab/`
- 忽略 `VLN-CE/`
- 忽略缓存和 `__pycache__/`

## 后续建议

如果后续继续整理，建议按下面方向推进：

1. 给 `tools/` 再细分为 `video/`、`dataset/`、`debug/`
2. 给 `qwen_annotation/` 增加更清晰的 `docs/` 或 `notes/`
3. 如果需要长期维护，可以增加 `CHANGELOG.md`
4. 如果后续要多人协作，建议补一次初始 commit
